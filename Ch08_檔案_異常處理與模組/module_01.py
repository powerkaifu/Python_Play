'''
模組:一個 .py 檔案就是一個模組
套件:相關模組放在同一個資料夾，稱為套件

# 語法
import module_name (as alias) # 直接導入模組，可以為模組取別名
from module_name import func1, func2, ... # 從模組中導入指定的函數

import package_name.module_name (as alias) # 直接導入套件中的模組，也可以為模組取別名
from package_name import module_name (as alias) # 從套件中導入指定的模組，也可以為模組取別名
'''
